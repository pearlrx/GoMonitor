database:
  host: db
  port: 5432
  user: postgres
  password: 12345
  name: gomonitor

collector:
  intervalSeconds: 20

#Here are the servers that we will monitor (Testing stand)
servers:
   - name: backend-server1
     address: host.docker.internal:8080
     description: Server back-end with users
   - name: backend-server2
     address: host.docker.internal:8060
     description: Test server